rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tillad alle loggede brugere at læse og skrive alle samlinger.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
